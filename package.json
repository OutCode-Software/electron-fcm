{
  "name": "aegix",
  "version": "1.0.0",
  "description": "aegix",
  "productName": "aegix",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder",
    "ele-build": "electron-builder",
    "build-electron": "rm -rf dist/ && yarn ele-build --config electron-builder-conf.js"

  },
  "build": "./electron-builder-config.js",
  "keywords": [
    "demo",
    "FCM",
    "push messages"
  ],
  "author": "aegix",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "12",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "electron-push-receiver": "^2.1.3",
    "electron-tray": "^0.1.1",
    "firebase": "^10.11.1",
    "rimraf": "^5.0.7"
  }
}
